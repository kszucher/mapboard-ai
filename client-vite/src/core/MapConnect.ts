import {M} from "../state/MapStateTypes";

export const mapConnect = (m: M, fromNodeSide: string, toNodeSide: string) => {

  let connectorStartX = 0
  switch (fromNodeSide) {
    // case 'left': connectorStartX =
  }


  // g. blahblah =
}
