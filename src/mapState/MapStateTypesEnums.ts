export enum Side {
  L = 'left',
  R = 'right',
  T = 'top',
  B = 'bottom',
}

export enum ControlType {
  NONE = '',
  FILE = 'file',
  INGESTION = 'ingestion',
  EXTRACTION = 'extraction',
}

export enum SubProcessType {
  INGESTION = 'ingestion',
  EXTRACTION = 'extraction',
}
